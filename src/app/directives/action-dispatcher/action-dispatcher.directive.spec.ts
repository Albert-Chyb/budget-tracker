import { ActionDispatcherDirective } from './action-dispatcher.directive';

describe('ActionDispatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionDispatcherDirective();
    expect(directive).toBeTruthy();
  });
});
