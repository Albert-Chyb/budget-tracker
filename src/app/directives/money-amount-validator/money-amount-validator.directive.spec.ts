import { MoneyAmountValidatorDirective } from './money-amount-validator.directive';

describe('MoneyAmountValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyAmountValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
