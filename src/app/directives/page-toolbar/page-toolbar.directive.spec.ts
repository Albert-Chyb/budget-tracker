import { PageToolbarDirective } from './page-toolbar.directive';

describe('PageToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new PageToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
