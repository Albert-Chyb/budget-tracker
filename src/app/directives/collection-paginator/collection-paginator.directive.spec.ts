import { CollectionPaginatorDirective } from './collection-paginator.directive';

describe('CollectionPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectionPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
