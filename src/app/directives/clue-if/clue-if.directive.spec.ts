import { ClueIfDirective } from './clue-if.directive';

describe('ClueIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ClueIfDirective();
    expect(directive).toBeTruthy();
  });
});
