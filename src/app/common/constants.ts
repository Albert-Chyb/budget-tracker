/**
 * Max number that a property with money amount can contain.
 * This amount is not given as the smallest nominal.
 * Multiply it by 100 to get maximum number that can be saved in the database.
 */
export const MAX_MONEY_AMOUNT_VALUE = 1_000_000;
